
# Library Sort Demo in Java

This Java program demonstrates sorting of integers and book names using built-in Java utilities.

## ✨ Features
- Accepts an array of integers and sorts them using `Arrays.sort()`
- Accepts a list of book names and sorts them using `Collections.sort()`
- Displays both unsorted and sorted data

## 🚀 How to Compile and Run

```bash
javac LibrarySortDemo.java
java LibrarySortDemo
